# MariaDB config recomendaciones
[mysqld]
# Escuchar solo en la red interna de Docker, no en interfaces públicas
bind-address=0.0.0.0
port=3306

# Seguridad: no permitir usuarios anónimos
skip-name-resolve
symbolic-links=0

# Usuarios y permisos restringidos (crear en el SQL de inicialización)
